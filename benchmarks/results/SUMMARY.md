# PLOT3D I/O benchmark: pooled summary

Generated by `aggregate_results.py` from every result file in `benchmarks/results/`.

**Machines reporting: 1** (`lptlib-ci-xeon-2c`). Result files pooled: 3.

> Only one machine has reported. Every across-machine range below collapses to that machine's single value, and the ranges are marked `single machine` rather than pretending to a spread. Absolute times on this benchmark move by more than an order of magnitude between machines, so treat the absolutes as indicative of this machine only. The ratios within a matched-work group are the transferable quantity. See `benchmarks/README.md` for how to contribute a second machine.

Each reader's per-machine number is a **median** over repeated reads after discarded warmups; the spread within a machine is its interquartile range. The columns below pool those per-machine medians: `median across machines` is the median of them, `range across machines` is min to max. Ratios carry a per-machine 95 percent percentile bootstrap interval on the ratio of medians, and the widest of those is shown so within-machine uncertainty is visible next to between-machine spread.

Buckets are never mixed: a warm-cache result and a cold-cache result measure different things, and so do two different grid sizes.

---

## ~2M-point grid, cold cache

2,039,250 points, 24.5 MB on disk (single precision), 4 blocks. Machines in this bucket: 1 (`lptlib-ci-xeon-2c`).

Whole-file read throughput measured during this run (the I/O floor reader, the direct test of the declared cache state): `lptlib-ci-xeon-2c` 2.49 GB/s. Page-cache reads run at gigabytes per second; device reads at hundreds of megabytes per second.

> Cache-residency check **failed** on: `lptlib-ci-xeon-2c` (declared cold but the I/O floor ran at 2.49 GB/s, which is page-cache speed, so the reads were not cold (in a VM the hypervisor's cache survives a guest drop_caches)). The declared cache state does not match what the machine actually did, so these numbers mix warm and cold reads. Treat them as a lower bound on this machine's performance, not as a clean warm-cache measurement.

> Dispersion warning: interquartile range exceeded 15 percent of the median for `fortran_raw` on `lptlib-ci-xeon-2c`, `lptlib_read_grid` on `lptlib-ci-xeon-2c`. Those numbers are indicative rather than tight.

### Reader times

**Matched work, single precision (C-contiguous block arrays)**

Every reader in this group ends with the same bytes in the same layout: one C-contiguous float32 `(ni, nj, nk, 3)` array per block. The comparison is like-for-like.

| Reader | Machines | Median across machines (ms) | Range across machines (ms) | Worst within-machine rel. IQR |
|---|---|---|---|---|
| lptlib strided read (f32, C-contiguous blocks) | 1 | 43.4 | single machine | 11.8% |
| naive Python nested-loop (f32, C-contiguous blocks) | 1 | 994.7 | single machine | 5.5% |
| Fortran stream read + transpose (f32, C-contiguous) | 1 | 32.3 | single machine | 10.0% |

**Matched work, double precision (padded array plus per-block bounds)**

Every reader in this group produces the padded float64 `(nimax, njmax, nkmax, 3, nb)` array and the per-block coordinate bounds that `GridIO.read_grid` returns. This is what the library actually costs.

| Reader | Machines | Median across machines (ms) | Range across machines (ms) | Worst within-machine rel. IQR |
|---|---|---|---|---|
| lptlib GridIO.read_grid (f64 padded + bounds) | 1 | 74.3 | single machine | 65.3% |
| Fortran stream read + padded f64 + bounds | 1 | 126.2 | single machine | 6.6% |

**I/O floor (diagnostic only, NOT a language comparison)**

Both rows only stream bytes into a column-major array and reorder nothing. They exist to show how much of each real reader's time is file I/O rather than memory reordering. Do not quote this group as a Python-versus-Fortran result.

| Reader | Machines | Median across machines (ms) | Range across machines (ms) | Worst within-machine rel. IQR |
|---|---|---|---|---|
| numpy raw read (f32 views, no reorder) | 1 | 9.8 | single machine | 12.9% |
| Fortran raw stream read (f32, no reorder) | 1 | 16.1 | single machine | 16.0% |

### Ratios (the transferable result)

Every ratio compares two readers doing **matched work**. A ratio above 1 means the lptlib/numpy side is faster.

| Comparison | Machines | Median ratio | Range across machines | Widest per-machine 95% CI | Reads as |
|---|---|---|---|---|---|
| lptlib strided speedup over the naive Python reader | 1 | 22.94 | single machine | [20.85, 24.00] | lptlib strided 22.94x faster |
| lptlib strided vs Fortran, matched f32 C-contiguous work | 1 | 0.74 | single machine | [0.69, 0.82] | lptlib strided 1.34x slower |
| lptlib read_grid vs Fortran, matched f64 padded+bounds work | 1 | 1.70 | single machine | [1.41, 1.82] | lptlib read_grid 1.70x faster |
| numpy vs Fortran at the I/O floor, no reorder either side | 1 | 1.64 | single machine | [1.48, 1.87] | numpy raw read 1.64x faster |

---

## ~2M-point grid, warm cache

2,039,250 points, 24.5 MB on disk (single precision), 4 blocks. Machines in this bucket: 1 (`lptlib-ci-xeon-2c`).

Whole-file read throughput measured during this run (the I/O floor reader, the direct test of the declared cache state): `lptlib-ci-xeon-2c` 5.02 GB/s. Page-cache reads run at gigabytes per second; device reads at hundreds of megabytes per second.

### Reader times

**Matched work, single precision (C-contiguous block arrays)**

Every reader in this group ends with the same bytes in the same layout: one C-contiguous float32 `(ni, nj, nk, 3)` array per block. The comparison is like-for-like.

| Reader | Machines | Median across machines (ms) | Range across machines (ms) | Worst within-machine rel. IQR |
|---|---|---|---|---|
| lptlib strided read (f32, C-contiguous blocks) | 1 | 35.0 | single machine | 10.0% |
| naive Python nested-loop (f32, C-contiguous blocks) | 1 | 973.3 | single machine | 6.0% |
| Fortran stream read + transpose (f32, C-contiguous) | 1 | 25.0 | single machine | 6.1% |

**Matched work, double precision (padded array plus per-block bounds)**

Every reader in this group produces the padded float64 `(nimax, njmax, nkmax, 3, nb)` array and the per-block coordinate bounds that `GridIO.read_grid` returns. This is what the library actually costs.

| Reader | Machines | Median across machines (ms) | Range across machines (ms) | Worst within-machine rel. IQR |
|---|---|---|---|---|
| lptlib GridIO.read_grid (f64 padded + bounds) | 1 | 64.8 | single machine | 14.4% |
| Fortran stream read + padded f64 + bounds | 1 | 113.5 | single machine | 8.2% |

**I/O floor (diagnostic only, NOT a language comparison)**

Both rows only stream bytes into a column-major array and reorder nothing. They exist to show how much of each real reader's time is file I/O rather than memory reordering. Do not quote this group as a Python-versus-Fortran result.

| Reader | Machines | Median across machines (ms) | Range across machines (ms) | Worst within-machine rel. IQR |
|---|---|---|---|---|
| numpy raw read (f32 views, no reorder) | 1 | 4.9 | single machine | 5.4% |
| Fortran raw stream read (f32, no reorder) | 1 | 12.5 | single machine | 5.8% |

### Ratios (the transferable result)

Every ratio compares two readers doing **matched work**. A ratio above 1 means the lptlib/numpy side is faster.

| Comparison | Machines | Median ratio | Range across machines | Widest per-machine 95% CI | Reads as |
|---|---|---|---|---|---|
| lptlib strided speedup over the naive Python reader | 1 | 27.80 | single machine | [26.09, 28.76] | lptlib strided 27.80x faster |
| lptlib strided vs Fortran, matched f32 C-contiguous work | 1 | 0.71 | single machine | [0.69, 0.75] | lptlib strided 1.40x slower |
| lptlib read_grid vs Fortran, matched f64 padded+bounds work | 1 | 1.75 | single machine | [1.68, 1.85] | lptlib read_grid 1.75x faster |
| numpy vs Fortran at the I/O floor, no reorder either side | 1 | 2.57 | single machine | [2.51, 2.62] | numpy raw read 2.57x faster |

---

## ~28M-point grid, warm cache

28,160,400 points, 337.9 MB on disk (single precision), 4 blocks. Machines in this bucket: 1 (`lptlib-ci-xeon-2c`).

Whole-file read throughput measured during this run (the I/O floor reader, the direct test of the declared cache state): `lptlib-ci-xeon-2c` 0.41 GB/s. Page-cache reads run at gigabytes per second; device reads at hundreds of megabytes per second.

> Cache-residency check **failed** on: `lptlib-ci-xeon-2c` (I/O floor ran at 0.41 GB/s, device rather than page-cache throughput; only 23% of the whole-file read throughput survived the run, so the grid file was evicted part way through). The declared cache state does not match what the machine actually did, so these numbers mix warm and cold reads. Treat them as a lower bound on this machine's performance, not as a clean warm-cache measurement.

> Dispersion warning: interquartile range exceeded 15 percent of the median for `fortran_contig` on `lptlib-ci-xeon-2c`, `fortran_full` on `lptlib-ci-xeon-2c`, `numpy_raw_read` on `lptlib-ci-xeon-2c`. Those numbers are indicative rather than tight.

### Reader times

**Matched work, single precision (C-contiguous block arrays)**

Every reader in this group ends with the same bytes in the same layout: one C-contiguous float32 `(ni, nj, nk, 3)` array per block. The comparison is like-for-like.

| Reader | Machines | Median across machines (ms) | Range across machines (ms) | Worst within-machine rel. IQR |
|---|---|---|---|---|
| lptlib strided read (f32, C-contiguous blocks) | 1 | 2214.4 | single machine | 12.8% |
| naive Python nested-loop (f32, C-contiguous blocks) | 1 | 30665.5 | single machine | 1.6% |
| Fortran stream read + transpose (f32, C-contiguous) | 1 | 1026.6 | single machine | 19.4% |

**Matched work, double precision (padded array plus per-block bounds)**

Every reader in this group produces the padded float64 `(nimax, njmax, nkmax, 3, nb)` array and the per-block coordinate bounds that `GridIO.read_grid` returns. This is what the library actually costs.

| Reader | Machines | Median across machines (ms) | Range across machines (ms) | Worst within-machine rel. IQR |
|---|---|---|---|---|
| lptlib GridIO.read_grid (f64 padded + bounds) | 1 | 3346.0 | single machine | 9.9% |
| Fortran stream read + padded f64 + bounds | 1 | 2545.0 | single machine | 23.7% |

**I/O floor (diagnostic only, NOT a language comparison)**

Both rows only stream bytes into a column-major array and reorder nothing. They exist to show how much of each real reader's time is file I/O rather than memory reordering. Do not quote this group as a Python-versus-Fortran result.

| Reader | Machines | Median across machines (ms) | Range across machines (ms) | Worst within-machine rel. IQR |
|---|---|---|---|---|
| numpy raw read (f32 views, no reorder) | 1 | 822.5 | single machine | 83.6% |
| Fortran raw stream read (f32, no reorder) | 1 | 205.1 | single machine | 14.8% |

### Ratios (the transferable result)

Every ratio compares two readers doing **matched work**. A ratio above 1 means the lptlib/numpy side is faster.

| Comparison | Machines | Median ratio | Range across machines | Widest per-machine 95% CI | Reads as |
|---|---|---|---|---|---|
| lptlib strided speedup over the naive Python reader | 1 | 13.85 | single machine | [13.12, 14.56] | lptlib strided 13.85x faster |
| lptlib strided vs Fortran, matched f32 C-contiguous work | 1 | 0.46 | single machine | [0.43, 0.48] | lptlib strided 2.16x slower |
| lptlib read_grid vs Fortran, matched f64 padded+bounds work | 1 | 0.76 | single machine | [0.69, 0.81] | lptlib read_grid 1.31x slower |
| numpy vs Fortran at the I/O floor, no reorder either side | 1 | 0.25 | single machine | [0.17, 0.36] | numpy raw read 4.01x slower |

---

## Machines

| Label | OS / kernel | CPU | Cores | RAM (GiB) | Filesystem | Storage | Python | NumPy | gfortran | Flags |
|---|---|---|---|---|---|---|---|---|---|---|
| `lptlib-ci-xeon-2c` | Linux 6.18.44-fc-v21 | Intel(R) Xeon(R) Processor @ 2.10GHz | 2 | 7.84 | ext4 | rotational | 3.11.15 | 2.4.6 | GNU Fortran | `-O2` |

## Result files pooled

- `lptlib-ci-xeon-2c_2Mpts_cold.json` -- 2026-08-21T16:57:08Z, 15 reps (+3 warmups discarded), naive reader 3 reps, 0 priming reads, cache `cold`, checksum verified: True
- `lptlib-ci-xeon-2c_2Mpts_warm.json` -- 2026-08-21T16:31:24Z, 31 reps (+5 warmups discarded), naive reader 9 reps, 3 priming reads, cache `warm`, checksum verified: True
- `lptlib-ci-xeon-2c_28Mpts_warm.json` -- 2026-08-21T16:53:47Z, 31 reps (+5 warmups discarded), naive reader 5 reps, 3 priming reads, cache `warm`, checksum verified: True

